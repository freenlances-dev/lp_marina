---
import '../styles/global.css';
import aboutContent from '../data/aboutContent.json';
import AboutCard from './aboutCard.astro';
---

<section class="bg-neutral-0">
  <div class="flex flex-col lg:flex-row gap-8 w-full py-24 lg:px-48 px-6">
    <div>
      <p class="font-poppins text-base text-neutral-600 uppercase">Sobre</p>
      <h3 class="font-playfair font-medium text-6xl">
        {aboutContent.about_title}
      </h3>
    </div>
    <div class="text-xl font-poppins">
      <div class="space-y-6">
        <p>{aboutContent.about_content_p1}</p>
        <p>{aboutContent.about_content_p2}</p>
      </div>
      <div class="flex flex-col lg:flex-row gap-4 mt-8">
        {
          aboutContent.aboutCard.map((about) => (
            <AboutCard
              icon={about.icon}
              title={about.title}
              description={about.description}
            />
          ))
        }
      </div>
    </div>
  </div>
</section>
